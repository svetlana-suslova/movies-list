import React from 'react';
import styled from 'styled-components';

const FilterBar = () => {
    return (
        <div>
            FilterBar
        </div>
        
    );   
}
export default FilterBar;